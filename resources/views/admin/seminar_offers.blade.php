@extends('layouts.app')
@section('content')
<div class="container"><div class="card border-secondary"><div class="card-header text-white bg-secondary">Seminer verme başvuruları</div><div class="card-body">@if(session('success-status'))<div class="alert alert-success">{{ session('success-status') }}</div>@endif<table class="table"><thead><tr><th>Başvuran</th><th>Konu</th><th>Tür</th><th>Özgeçmiş</th><th>Durum</th><th>İşlem</th></tr></thead><tbody>@forelse($seminarOffers as $offer)<tr><td>{{ $offer->user->name }} {{ $offer->user->surname }}<br><small>{{ $offer->user->email }}</small></td><td>{{ $offer->seminarSubject?->subject ?? $offer->seminarSubjectProposal?->subject }}</td><td>{{ ['in_person'=>'Yüz yüze','online'=>'Online','either'=>'Fark etmez'][$offer->seminar_type] }}</td><td>{{ $offer->biography }}</td><td>{{ $offer->status }}</td><td>@if($offer->seminarSubjectProposal?->status === 'pending')<form method="POST" action="{{ route('admin.seminar-subject-proposals.accept', $offer->seminarSubjectProposal) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-success">Konuyu kabul et</button></form>@endif</td></tr>@empty<tr><td colspan="6">Henüz başvuru yok.</td></tr>@endforelse</tbody></table></div></div></div>
@endsection
